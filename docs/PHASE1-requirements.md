# 阶段一 · 需求分析与现状审计

> 目标：在动手前明确"现在有什么、缺什么、按什么顺序补"，避免返工。
> 审计基线：`AI_Todo_Assistant_开发指南_V1.1_优化版.md`（唯一事实来源）+ 现有骨架代码（38 文件）。

## 1. 现状快照
- 骨架已覆盖 **MVP1 零 AI 版** 全部后端能力：鉴权（签名 Cookie + PBKDF2）、Task CRUD、规则引擎复盘、企微推送、三个 Cron、D1 迁移、Seed、基础前端 SPA。
- 仓库已初始化（`main` + `develop`），含 `.github` CI、分支策略、CONTRIBUTING。
- 此前评审出的 12 条 Issues 中，#4（每日手动 AI 计数持久化）**代码已落地**（`countManualAiToday` + `recordAiUsage`）。

## 2. 需求追溯矩阵（12 条 Issues 状态）
| # | 需求 | 现状 | 依赖 | 风险 |
|---|---|---|---|---|
| 1 | Workers AI 模型 ID 占位符 | 占位 `<deployment-verified-model-id>` | 无 | 低（AI 默认关闭） |
| 2 | 企微 Markdown 子集渲染验证 | 渲染函数存在，但"生成模式"标签写死、无子集校验 | #3 之后 | 中 |
| 3 | 限流改为 KV/D1 跨实例 | **仍为单实例内存 Map** | 无 | **高（安全）** |
| 4 | 持久化每日手动 AI 计数 | ✅ 已落地 | — | — |
| 5 | 完善移动端页面 | 仅基础 SPA | API 稳定 | 中 |
| 6 | 导出/备份脚本 | 缺失 | 无 | 中 |
| 7 | 集成/安全测试 | 仅 3 个单测 | 无 | 中 |
| 8 | 多环境配置 | 缺失 | 无 | 低 |
| 9 | 关键指标告警 | 文档化，未接 | 部署后 | 低 |
| 10 | MVP4 备用适配器 | 预留接口，无实现 | MVP4 | 低 |
| 11 | 文档—代码一致性巡检 | 部分偏离（模型 ID/限流） | — | 低 |
| 12 | Seed 密码策略与密钥分发 | 无策略校验 | #3 之后 | 中（安全） |

## 3. 功能依赖关系
```
[DB 迁移] ──> [D1 限流 #3] ──> [登录安全]
                     │
[Seed 策略 #12] ────┘
[模型 ID #1] ──> [AI 增强启用开关]
[Markdown 校验 #2] ──> [推送可靠性]
[多环境 #8] ──> [部署流水线]
[导出脚本 #6] ──> [运维验收清单]
[前端 #5 / 测试 #7] ──> 验收体验与质量门禁
```

## 4. 风险驱动的优先级（本周期执行顺序）
1. **#3 限流 D1 化** — 最高风险：多实例下内存计数不共享，暴力登录防护在生产形同虚设。
2. **#12 Seed 密码策略** — 中风险：首账号弱密码直接击穿鉴权。
3. **#1 模型 ID 默认化** — 低风险但阻塞部署验收，顺手解决。
4. **#2 Markdown 校验 + 真实来源标签** — 中风险：错误渲染/误导状态。
5. **#6 导出/备份脚本** — 验收清单硬要求。
6. **#8 多环境配置** — 部署流水线前置。
7. **#5 前端增强** — 体验/验收，依赖 API 已稳定。
8. **#7 测试扩展** — 质量门禁。

## 5. 本周期范围
- **实现**：#3 #12 #1 #2 #6 #8 #5(部分) #7(部分) #11(对齐)。
- **暂缓（非阻塞、记载待办）**：#9 监控接入（需部署后 Cloudflare 面板）、#10 MVP4 适配器（MVP4 阶段）、完整集成测试（需活动 Worker）。

## 6. 待办（延续到下一周期）
- [ ] 部署后在真实企微群人工核验 Markdown 渲染（#2 运行期验证）。
- [ ] Cloudflare Analytics 配置告警规则（#9）。
- [ ] MVP4 备用 Provider 适配器骨架（#10）。
- [ ] 用 Vitest + Miniflare 写可本地跑的集成测试（#7）。
- [ ] 将 Issues 清单落成 GitHub Issues（需目标仓库）。
